version: '3'

services:
  asp.net_core_2.0_web_application_web_mvc_with_docker:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
networks:
  default:
    external:
      name: nat
